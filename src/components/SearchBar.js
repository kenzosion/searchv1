import React, { Component } from 'react'

export default class SearchBar extends Component {
  render() {
    return (
      <React.Fragment>
        <form>
            <input type="text"/>
        </form>
      </React.Fragment>
    );
  }
}
