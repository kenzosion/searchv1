import React, { Component } from 'react'

export default class SearchBar extends Component {
  render() {
    return (
      <React.Fragment>
        <div>Search bar</div>
      </React.Fragment>
    )
  }
}
